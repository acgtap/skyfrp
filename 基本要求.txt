基本要求：

帮我使用Vue3+Tailwindcss+Axios做一个关于skyfrp内网穿透工具的前端界面，风格使用现代简约 Shadcn-ui风格，主色调：紫色（#7367f0），用于按钮、链接、表单元素等。背景颜色：浅灰色（#f8f7fa），为页面提供了一个柔和的背景。不要有明显的渐变色。所有文字只可以使用黑色或白色。
前端介绍界面包含软件名称，log，最上面有导航栏，点击可以跳转对应介绍，导航栏包含首页，介绍，定价，关于及帮助文档等，里面所有的在同一个界面。在首页中有我们的开始使用按钮跳转到管理面板，介绍页面里有我们的功能，包括个人建站，游戏联机，nas访问，远程桌面等，然后是关于界面，里面可以有服务器节点数，创建隧道数量，注册用户数量等统计数据，最后是定价帮我先暂时定一个后面修改，帮助文档界面可以简单的列几个常见问题，然后有一个查看帮助文档的按钮，点击后跳转到我们的帮助文档，需要单独开一个新的网页。
然后是管理面板部分，管理面板中在左侧也有导航栏，包括首页，个人资料，隧道管理（隧道管理为可以下拉，里面包括隧道列表，节点列表，配置文件和软件下载）、增值服务（里面有余额充值和余额商城）、用户日志、和帮助文档，帮助文档后跳转一个新的界面。现在我来一个界面一个界面的描述，每个界面的后端api基础url为 https://mskapi.cnwbhw.com/，我将剩下的api地址放到文件api.txt中，我会告诉你序号，你去对应的查看api接口地址及相应的说明。
首先是首页界面，我们需要在右上角有用户的基本信息包括昵称和头像，以及可以退出登录的退出功能，（当点击退出登录后就回到前端介绍界面）。基本信息旁边有签到按钮，13. 用户签到接口接口地址: /api/users_sign_in请求方法: POST用户每日可签到一次，获得流量奖励
首页界面里面有创建好了的隧道数量，总上传流量速度，下载流量速度，以及余额显示，这些信息下面用一个曲线图统计每天花费的流量，还有交流群及一些关于skyfrp的介绍。
然后是个人资料界面，首先我们获取到用户的全部信息，获取用户所有数据接口
接口地址: /api/get_users_all_data请求方法: POST
里面可以可以接收向用户发送的信息，以及卡密兑换，卡密兑换的接口地址为：5.卡密验证接口：/api/wyzz_kami_key，在api.txt文件中去查看详情。还需要有邮箱绑定，邮箱绑定接口为6. 绑定邮箱接口，接口地址: /api/bind_email
绑定邮箱后更新用户全部的数据7. 更新用户全部数据接口接口地址: /api/reboot_users_data，个人资料界面可以显示用户id，用户uid，用户剩余流量，用户余额等等，根据返回的数据生成，
然后是隧道管理界面，在隧道列表里面，当没有创建隧道时，页面只显示创建你的第一个隧道，如果有隧道的话就在列表右下角显示创建隧道，相关的api有11. 获取用户隧道列表接口接口地址: /api/get_frp_tunnel_list请求方法: POST
10. 创建FRP隧道接口接口地址: /api/create_frp_tunnel请求方法: POST
9. 删除FRP隧道接口接口地址: /api/delete_frp_tunnel请求方法: POST
用户先创建隧道，创建时可以选择相应的服务器节点，创建好后通过获取用户创建好的隧道信息，然后在前端显示出来，用户也可以删除隧道，删除隧道后再去获取用户隧道信息然后前端显示。
在服务器节点列表里面，显示有的服务器及相关的服务器数据，8. 获取FRP节点配置接口接口地址: /api/get_frp_node_config请求方法: POST配置文件和软件下载界面先空着，列出基本信息，后面再改
增值服务界面里，余额充值的接口为：用户充值接口 【直接跳转去支付网站】【请求方法：get 外网请求】 http://msk.cnwbhw.com/service/pay/mfzz/epayapi.php?type= 支付方式&money=金额&name=用户ID&out_trade_no=商户订单号 支付方式有：wxpay【微信支付】 alipay【支付宝支付】 金额就是充值金额 用户ID就是用户的ID 商户订单号为纯数字如123456789 测试数据{ 请求： http://msk.cnwbhw.com/service/pay/mfzz/epayapi.php?type=wxpay&money=10&name=1&out_trade_no=1234566524321  【请求后会跳转到支付网站】 }
余额商城的api为20. 余额购买商品接口接口地址: /api/shop_purchase请求方法: POST
用户日志界面，我们需要记录用户的操作，比如用户登录，需要记录用户ip和用户地址，包括时间等，还有用户充值及余额购买等信息，需要用到的api到api.txt里面去找，前置url均为https://mskapi.cnwbhw.com/
点击管理面板后，首先需要进行登录，使用qq登录即可，后端api我稍后粘贴给你，所有前置URL均为http://43.242.193.146:8080后续我不给前置URL了。QQ登录的后端api接口地址为：/api/qq_login_url，此接口用于获取QQ登录的跳转URL
- 生成的URL包含随机的state参数，用于防止CSRF攻击
- 用户需要跳转到返回的login_url进行QQ授权登录
- 授权成功后，QQ会重定向到配置的redirect_uri（即/auth/qq-callback）
- 回调接口会自动处理用户登录/注册，并重定向到前端页面
- 前端页面应准备好接收users_id和temp_key参数
登录后QQ登录回调接口的接口地址: /auth/qq-callback请求方法: GET
在回调时需要带上redirect_uri参数，
请求参数:
  code: string, 必填, QQ返回的授权码
  state: string, 可选, 防止CSRF攻击的状态参数
处理流程:
  1. 使用code获取access_token
  2. 使用access_token获取用户信息（openid）
  3. 检查用户是否已存在（使用openid作为users_uid）
  4. 用户存在则登录，不存在则注册
  5. 重定向到前端页面
重定向URL:
  成功: https://csmsk.cnwbhw.com/auth-success?users_id=xxx&temp_key=xxx
  失败: https://csmsk.cnwbhw.com/login?error=错误类型
错误类型:
  missing_code: 缺少授权码参数
  token_failed: 获取access_token失败
  auth_failed: 授权失败
  invalid_response: 响应数据不完整
  network_error: 网络连接错误
  user_info_failed: 获取用户信息失败
  user_info_error: 用户信息错误
  database_error: 数据库连接失败
  user_banned: 用户已被封禁
  create_failed: 用户创建失败
  server_error: 服务器内部错误
  system_error: 系统错误
说明:
- 此接口是QQ登录的回调接口，由QQ服务器在用户授权后自动调用
- 处理成功后，会重定向到前端指定的页面，并携带用户ID和临时密钥
- 使用openid作为用户的唯一标识符（users_uid）
- 新用户会自动获得5G用户流量和5G每月流量
- 流量有效期至2035-09-30 23:59:59
在登录完成后通过临时秘钥获取用户信息
获取用户所有数据接口
接口地址: /api/get_users_all_data
请求方法: POST
请求参数:
  user_temp_key: string, 必填, 临时密钥
响应数据:
  {
    "code": 0,
    "msg": "获取用户数据成功",
    "timestamp": "2024-01-01T12:00:00",
    "data": {
      "user_info": {
        "users_uid": "用户UID",
        "users_name": "用户名",
        "user_group": "用户组",
        "users_email": "用户邮箱",
        "user_money": "用户余额"
      },
      "traffic_info": {
        "total_traffic": 总流量,
        "traffic_used": 已使用流量,
        "available_traffic": 可用流量
      }
    }
  }
获取到用户的相关数据后在管理面板中个人资料页面显示出来。


注意：// 用户认证相关API ，必须返回redirect_uri参数，
export const authAPI = {
  // 获取QQ登录URL
  getQQLoginUrl: (redirectUri = null) => {
    console.log('调用获取QQ登录URL接口, redirectUri:', redirectUri)
    if (redirectUri) {
      return api.get('/api/qq_login_url', {
        params: { redirect_uri: redirectUri }
      })
    } else {
      return api.get('/api/qq_login_url')
    }
  },
  参考网站https://www.mefrp.com/ 
https://www.chmlfrp.net/ 
https://frp.starryfrp.com/ 
https://moe.al/ 

QQ登录流程图
